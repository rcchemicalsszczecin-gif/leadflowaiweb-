# LEADFLOWAI REPOSITORY STATUS

DATE: 2026-08-13

BRAND=LEADFLOWAI
DOMAIN=leadflowai.pl
PUBLIC_EMAIL=kontakt@leadflowai.pl
FOUNDATION_STAGE=PASS
DESIGN_DIRECTION=APPROVED_V1
APPLICATION_SCAFFOLD=PASS
DESIGN_SYSTEM=IMPLEMENTED_V1
HOMEPAGE=IMPLEMENTED_V1
CORE_MONEY_PAGES=PASS
SEO_AEO_GEO_DEEP_FOUNDATION=PASS
CONTACT_UI=PASS
LEAD_FORM_DELIVERY=OFF_BY_OWNER
DIRECT_EMAIL_CONTACT=PASS
EMAIL_ROUTING=PASS
CHATBOT_OFFER=YES
CHATBOT_PUBLIC_UI=OFF
CHATBOT_CODE=DORMANT_READY
PORTFOLIO_KNOWLEDGE=PASS
STATIC_FRONTEND=PASS
GITHUB_PAGES_ARTIFACT=PASS
GITHUB_PAGES_SOURCE=GITHUB_ACTIONS
GITHUB_PAGES_WORKFLOW=READY_NOT_DEPLOYED
CUSTOM_DOMAIN=PASS
CLOUDFLARE_DNS=PASS
HTTPS=PASS
FUTURE_CHAT_API_ORIGIN=https://api.leadflowai.pl
LOCAL_CHAT_API=NOT_STARTED
CLOUDFLARE_TUNNEL=DEFERRED_WITH_CHATBOT
ANALYTICS=PENDING_CONFIGURATION
LEGAL_FINALIZATION=PENDING_COMPANY_REGISTRATION
PRODUCTION_SITE_CONTENT=NOT_DEPLOYED
MAIN_MUTATED=YES_CNAME_ONLY
WORK_BRANCH=build/leadflowai

## Static frontend + GitHub Pages

Validated static-export architecture:
- Next.js uses static export with `output: "export"`;
- dynamic frontend Route Handlers do not remain under `app/api/**`;
- `out/` is generated by the build;
- `out/CNAME` identifies `leadflowai.pl`;
- `out/.nojekyll` is present;
- sitemap and robots export statically;
- representative public routes passed static HTTP smoke tests in prior CI;
- Pages deployment workflow triggers from `main` and publishes the generated `out/` artifact.

GitHub Pages repository configuration was independently verified on 2026-08-13 as `build_type=workflow`, custom domain `leadflowai.pl`, approved HTTPS certificate and HTTPS enforcement.

## Contact owner decision

Inbound mail routing for `kontakt@leadflowai.pl` is configured and was manually tested successfully.

No outgoing-mail service or online lead delivery backend is active. The public contact UI must use direct e-mail only and must not expose a non-functional submit button. `scripts/lead-contract.mjs` enforces this state until Owner explicitly reopens the form-delivery stage.

## Chatbot owner decision

AI chatbots remain part of the LeadFlowAI commercial offer. The LeadFlowAI site chatbot itself is intentionally disabled until explicit Owner configuration/enablement. The dormant component and future local-AI boundary stay in the repository, but `app/layout.tsx` must not mount `SiteAssistant` or load `chat.css` while this decision remains active.

## Current architecture boundary

Public frontend target: GitHub Pages.
DNS/TLS/edge: Cloudflare.
Public mail: `kontakt@leadflowai.pl` through Cloudflare Email Routing.
Future dynamic chatbot API target: `api.leadflowai.pl` on Owner-controlled local hardware through Cloudflare Tunnel, only if/when the public chatbot is enabled.

The production application code has not yet been promoted from `build/leadflowai` to `main`.
